apiVersion: v1
kind: Service
metadata:
  name: postgres-svc
  namespace: app
  labels:
    app: postgres-db
    tier: data-tier
spec:
  type: ClusterIP
  selector:
    app: postgres-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
