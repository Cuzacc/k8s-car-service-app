apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: app
data:
  NODE_ENV: "production"
  PORT: "5000"
  DB_HOST: "postgres-svc"
  DB_PORT: "5432"
  DB_NAME: "autocare_db"
  DB_USER: "car_admin"
